# Plutus — Activity Log

Running log of operations performed on this app repo: code, build, ops,
verification. Lives here (not in the Alena OS vault) because `app/` is its own
git repo — this log travels with the code.

**Scope:** anything inside `app/`. Work on vault-side project files
(`finance-tracker-plan.md`, wireframes, market research) belongs in the vault's
`00_system/activity.md` instead.

**Rule:** per `AGENTS.md` §3 — demo/placeholder numbers only. Never a real
balance, never a Keychain secret value, never a real institution's data.

Tags: `[GIT]` `[FILE]` `[CONFIG]` `[OPS]`
Format: `### YYYY-MM-DD` then one line per action with tag prefix.

---

## Open security items

(none)

### 2026-08-01 (later)

- `[FILE]` **LICENSE added — MIT**, copyright "Ai Ling (Alena) You". Chosen over AGPL: framing is permanently-free portfolio project with no monetization path to protect, and MIT matches the borjasolerme coding-workflow skill the repo's practice section already binds to. Unblocks public use — the repo had been public with no license, which legally permits nobody to use, fork, or redistribute it.
- `[FILE]` **SECURITY.md written** — data-flow page for the audience-A (self-hoster/dev) launch. Covers: where each artifact lives (DB / db key / Plaid creds / PIN hash / access tokens) and what protects it; the complete list of outbound destinations (Plaid, plus optional Anthropic receiving merchant name strings only); localhost bind + 403 backstop; CSP and security headers; PIN/scrypt/timingSafeEqual/HKDF session design and the Keychain-persisted brute-force counter; CSRF origin rule incl. missing-Origin rejection; generic-500 error wrapping; gitleaks + the `xlsx` CDN-tarball provenance note; demo-mode namespace isolation; known limitations (macOS-only, FileVault dependency, no third-party audit). Every claim traced to the source file it lives in before writing — no unverified assertions.
- `[FILE]` README: added `## Security` section (one-paragraph summary + SECURITY.md link + `xlsx` provenance note) and `## License` section. Typecheck clean, 12/12 tests pass after.
- `[OPS]` **Repo-history audit before launch** (locked rule: never publish history containing pre-scrub real numbers). Result: **history is clean.** Root commit `9f97b91` is a bare Next.js scaffold (24 files, no data); zero `.db`/`.env`/`seed.local.json`/`.csv`/`.xlsx` blobs in any commit; zero currency amounts in any commit message; `lib/seed-config.ts` has zero 4+ digit numbers across all four of its versions and declares itself fake in its header. Real seed stays at `~/FinanceTracker/seed.local.json`, gitignored, never tracked. The repo was already re-rooted with fresh history — the rule was satisfied before this session.
- `[OPS]` **Two discrepancies for Alena to resolve.** (1) `github.com/alenabeex/plutus` reports `visibility: PUBLIC`, but the 08-01 and 07-27 `[GIT]` entries above both record it as private — the log and GitHub disagree; confirm which is intended. (2) `activity.md:54` describes a tile value with the phrasing "with her numbers", which reads like real figures rather than demo — per AGENTS.md §3 it needs review and likely a scrub. Values deliberately not reproduced here.
- `[OPS]` Uncommitted `middleware.ts` diff reviewed: renames the default export `proxy` → `middleware`. Cosmetic — Next.js dispatches on the default export, not its name, so the local-access guard was already live in the committed build. No security gap in what is published.

### 2026-08-01

- `[GIT]` Pushed `ad5e472..d31965d` to `github.com/alenabeex/plutus` (private), 2 commits: `820e6d1` middleware rename (the 07-31 local-access-guard fix, found staged but uncommitted — committed as its own commit) and `d31965d` README hero swap + activity log. Code and docs only — no financial data, no secrets; gitleaks clean on both.
- `[FILE]` README hero swapped to Net Worth (Alena's ask "use screenshot-networth.png for the main page"): hero now `docs/screenshot-networth.png`, and the "More screens" gallery's Net Worth column replaced with Cash Flow (`docs/screenshot-cashflow.png`) so all four tabs stay visible. Matches the locked IA framing — Net Worth is the design-template reference view. `docs/screenshot.png` left on disk, no longer referenced.

### 2026-07-31

- `[FILE]` **Activity log corrected (security).** 2026-07-27 note claimed "Local-access guard is claimed but not implemented" — incorrect. `proxy.ts` (Next.js 16 convention, not `middleware.ts`) has been active since v1 and IS enforcing: requests to non-localhost hosts → 403 "Forbidden — local access only", unless `FT_DEMO=1` + `FT_TUNNEL_HOST` set for demo tunnel. Mutating requests validated on Origin too (MISSING rejected). The host/origin/CSRF/CSP checks all live. Removed the stale "open security items" note.

### 2026-07-30

- `[FILE]` Nav row narrowed (Alena: "too wide" after the alignment fix): the header content matching main's full 1140px box read as too spread-out once brand/nav were pinned to the actual card edges — big empty gap to the far-right Allocation card. Restructured to two nested containers: an outer div does ONLY positioning (mx-auto max-w-1140 + main's exact padding, so it still starts at the same left edge as the cards), an inner `max-w-[660px]` div (left-anchored, no mx-auto) holds brand+nav and caps the row to roughly one card's width instead of the full two-card span. Verified: brand.left still 178.5 (= first card), nav.right 838.5 (≈ first card's 840.9, the ~2px gap is the hardcoded 660 vs that card's organic 662px). Typecheck + lint clean.
- `[FILE]` Net worth chart was a single dot in demo mode (Alena: "looks weird that it is just a dot") — `seed()` in `lib/db.ts` only ever wrote one `networth_snapshots` row (today's). Added a DEMO-only backfill: 13 biweekly points over ~6 months, gentle upward drift + small deterministic wobble (no `Math.random` — reproducible across reseeds), landing exactly on today's real seeded total. Gated strictly to `DEMO` (the `FT_DEMO=1` flag already used elsewhere in this file) — never touches the `seed.local.json` path, so a real install's history stays 100% genuine, one real day at a time; no fabricated data risk. Deleted `~/FinanceTracker/demo.db` and restarted the demo server to reseed (real `finance.db`/`seed.local.json` untouched); verified live — chart now reads "$6.6k (+10.2% all time)" with a real Jan→Jul line ending on the correct $71,239.35 dot. Typecheck + lint (0 errors) + test (12/12) clean after. Recaptured all 4 screenshots; `docs/screenshot-networth.png` updated in the README gallery. Left `docs/screenshot.png` (README hero) as Cash Flow — that was a separate earlier decision, not touched by this ask.
- `[FILE]` Header alignment bug fixed (Alena ask): `aside` (Plutus/nav) and `main` (cards) had padding in structurally different places — main's `px-4/sm:px-6/lg:px-9` lived INSIDE its `max-w-[1140px]` box, aside's lived OUTSIDE its matching inner box — so the header content sat 36px further left than the cards below it at desktop width, reading as an oversized nav row. Moved aside's padding onto the same inner div as its max-w, mirroring main exactly. Verified via getBoundingClientRect: brand/nav edges now match the first card's edges to the pixel (178.5/1246.5 both sides). Typecheck + lint clean.
- `[OPS]` End-to-end check before screenshotting (Alena ask: "line up the plutus end to end before you do"): `pnpm typecheck` clean · `pnpm lint` 0 errors (12 pre-existing warnings, matches known baseline) · `pnpm build` clean, all 13 routes compile · `pnpm test` found 1 real failure — `lib/__tests__/cashflow-view.test.ts` "groups income by payer" used a strict `.toEqual` fixture predating the 2026-07-29 `CashflowTxn.account` field addition, so it never got the new field. Fixed by adding `account: ""` to the two expected txn objects (fixture accounts table is empty, so the join legitimately resolves to `""`). 12/12 pass after.
- `[FILE]` All 4 tabs screenshotted (Alena ask), demo data: `docs/screenshot-networth.png`, `docs/screenshot-cashflow.png` (also `docs/screenshot.png`, the README hero), `docs/screenshot-subscriptions.png`, `docs/screenshot-connections.png`. Same headless-CDP method as 2026-07-29's hero shot, batched into one script/session (unlock once, click through all 4 nav items). First pass on Subscriptions came out with near-invisible calendar-chip text — investigated before treating it as a product bug: `lib/colors.ts` chip styling (INK on CARD/SOFT) is correct AA-contrast, and the Browser-pane render of the same view was fully legible. Root cause was headless-capture-specific: the CDP-created tab is never foregrounded, so Chrome throttles/freezes the entrance `sub-chip-in` CSS animation mid-fade regardless of wait time. Fixed the *capture script*, not the app — injects a stylesheet forcing `animation-duration:0s` before each shot so every view lands in its settled state. Recaptured all 4; verified. README updated: gallery section added (`<details>` with Net Worth/Subscriptions/Connections table) under the hero image; also corrected a README inaccuracy caught in passing — "Five tabs...Transactions" listed a tab that isn't shipped (Transactions is a Cash Flow drill-down, not a nav item) — now reads "Four tabs: Net Worth · Cash Flow · Subscriptions · Connections."
- `[FILE]` README hero screenshot (Alena ask: "best part of demo mode"): `docs/screenshot.png` — Cash Flow view, JUL 2026, demo data (Acme Corp income, 53% Great savings rate, Needs/Wants bars), captured headless (Chrome CDP, demo instance on 3000, demo PIN, Next dev badge stripped, 1440×980 @2x). Chose Cash Flow over Net Worth: demo Net Worth graph has one snapshot → "not enough history yet" reads unfinished. README image line switched on. Follow-up noted: seed demo snapshot history so Net Worth can be a second image.

### 2026-07-29

- `[FILE]` Public-repo docs set, modeled on the Open-Legal-Products/mike layout (Alena ask): README rewritten to that structure — one-line product statement, Contents, Prerequisites, Database Setup, Environment, Plaid Integration (their "CourtListener Integration" slot), Install, Run Locally, First Run, Troubleshooting, Roadmap, Useful Checks. New `CONTRIBUTING.md` (their headings; "no cloud/hosted refactors" mirrors their "no local-hosting refactors" line, plus design-token rules and the demo-data-only rule) and `.github/PULL_REQUEST_TEMPLATE.md` (verbatim structure, checklist adapted to financial data). Docs only, no code touched. Covers T11.4; README still names Plutus pending the rename decision.
- `[FILE]` Popover menus standardized on the Export-menu row style (Alena spec): new shared `MENU_ITEM` + `menuItemHover` in lib/styles.ts (full-width row, 9/12 padding, radius 10, SOFT hover). Txn popover pills → stacked rows: "Rename" and "Category · <current> ›"; Cash Flow Export menu switched to the shared const (local copy deleted); Connections' manual-asset (Edit…/Remove…) and institution (Re-sync/Remove…) menus converted too — every ⋯ popover in the app now hovers row-by-row identically. Typecheck clean; only 2 pre-existing connections warnings (verified vs stash). Page 200.
- `[FILE]` Txn popover → one line + destination picker modal (Alena spec): popover is now [Rename] [current-location ▾] on a single line — Rename swaps to an inline input (Enter saves, Esc cancels; new PUT action `"rename"` writes `merchant`, so display + future rule-learning key on the user's name); the pill opens a NEW TxnPickerModal instead of inline chips — destinations grouped under uppercase micro-labels Income · Needs · Wants · Savings · Disputed (current location omitted, same rule-learning path kept for same-area category changes). Plumbing simplified: categoryPickerOpen/onToggleCategoryPicker/onTxnAction props stripped from Row/TxnRow. Same pass (Alena mid-turn): category-row hover removed (txn rows keep it), drill-down guide line re-anchored to the avatar circle's center (ml-22px), and txn dates now start at 54px — flush with the category label's left edge. Rename verified live on demo API (rename + revert). Typecheck + lint clean; page 200.
- `[FILE]` Account column pulled left + hover states (Alena ask): drill-down columns go proportional (merchant flex-3 · account flex-2 · amount) so the account column starts at a consistent x nearer the merchant instead of hugging the amount; txn rows and category rows both get the allocation-legend hover treatment (SOFT background, rounded; category rows only when clickable). Typecheck + lint clean; page 200 on demo.
- `[FILE]` Txn source account gets its own aligned column (Alena ask): drill-down row goes columnar — date · merchant (flex, truncates) | account (fixed w-44, dimmed, truncates, hidden below sm) | amount (right). Accounts now line up vertically across rows instead of trailing the merchant inline. Typecheck + lint clean; page 200 on demo.
- `[FILE]` Layout centered (Alena ask): `main` gains `mx-auto w-full` on its existing `max-w-[1140px]`, and the header's brand+nav wrap in a matching centered `max-w-[1140px]` container — content and header share one centered column instead of hugging the left edge; below 1140px wide nothing changes. Typecheck + lint clean; page 200 on demo.
- `[FILE]` Drill-down txn rows now show their source account (Alena ask): `cashflowView` joins accounts (nickname wins over name, masked digits appended — 'Travel Card ···1187'); `CashflowTxn.account` added; row renders date · merchant · account at 75% opacity, blank when no account. Verified on demo API. Typecheck + lint clean.

### 2026-07-28

- `[FILE]` Saved tile re-semanticized once more (Alena spec, sixth pass): tile now shows the Savings section total (money actively put away), NOT leftover — leftover was invariant under savings↔expense moves (+600/−600 cancel), which read as "the top numbers don't update". Now moving $600 savings→expenses drops Saved by 600, raises Expenses by 600, and recomputes rate/grade on the same refetch. Verified live on demo API: move txn saved→expense shifted expenses/savedTile/rate-base exactly, then restored. Caveat noted: moving an INFLOW-typed txn into expenses acts as a refund (negative expense) — correct mechanics, garbage-in visible with demo's paycheck-in-savings row. Typecheck + lint clean.
- `[FILE]` Savings math corrected (Alena spec, fifth pass): Saved tile now shows LEFTOVER AFTER SAVING — income − expenses − savings-section (client-side `data.saved - data.totalSaved`; the old income−expenses value, e.g. $55.47, no longer renders — with her numbers the tile reads −$195). Savings rate + grade deliberately stay on income − expenses so moving money into Acorns can never lower the rate. Savings rows' share % re-based from section total (self-referential 100%) to INCOME — "Acorns = 6% of what I earned". Typecheck + lint clean; page 200 on demo.
- `[FILE]` Savings polish (Alena spec, fourth pass): section retitled Saved → "Savings" everywhere user-facing (card title, popover chip + current-location pill; the Saved TILE keeps its name — it's the leftover number, and the retitle finally de-collides the two). Both card edit buttons restyled to the Connections manual-assets bordered treatment (1px LINE border, radius 10, white bg, hover SOFT) — shared EDIT_BTN const. Savings card gets its own Edit/Done: in edit mode a destination row opens a modal that bulk-moves ALL its transactions to Income / a category / Disputed via the existing per-txn PUT (sequential; destinations are merchant groupings so "edit" = re-route the money, not rename bank labels). Typecheck + lint clean; page 200 on demo.
- `[FILE]` Category editing moved into a modal + rows get real icons (Alena spec, third pass): edit mode no longer shows per-row × — in edit mode a category row (pencil affordance) opens an "Edit category" modal with rename field (autofocus, Enter saves, name-clash validated server-side) and a two-step in-modal delete; backdrop/Escape close. New `PATCH /api/categories` (rename is label-only — rules/txns reference category_id, nothing recategorizes). Row avatars swap initials for keyword-matched lucide glyphs (Groceries→cart, Rent→building, Pets→paw, Crypto→bitcoin, …; income→banknote, saved→piggy bank fallback, disputed→flag, fallback→tag) so renamed/custom categories still get a sensible icon. Typecheck + lint clean; page serves 200 on demo.
- `[FILE]` Cash Flow rows get Net Worth-style identity + edit mode (Alena spec, second pass): every row (Income / Needs / Wants / Saved / Disputed) now leads with a 38px initials circle matching Net Worth's account avatars (T8.1 template); popover title simplified to "Category" with Disputed listed under the categories (red, last) and the standalone Dispute link gone; Escape closes the popover. New "Edit"/"Done" text toggle on the Expenses card header: edit mode shows a × per category row, two-step delete (arm → "Delete?" → commit) via new `DELETE /api/categories` — non-destructive, the category's transactions fall back to Uncategorized and its learned rules are removed so a sync can't resurrect the label. Typecheck + lint clean; page serves 200 on demo.
- `[FILE]` Txn popover redesigned to one control (Alena spec): "Move to" title + a pill showing where the txn currently lives (Income / Saved / Disputed / its category); the chevron expands EVERY destination — Income, Saved, Disputed (red), and all categories. Standalone "Dispute charge" link removed — Disputed is now just a destination, stored as the same `txn_class='excluded'` the old action wrote, so old and new disputes are one population. Disputed txns collect into a visible "Disputed" folder at the bottom of the Expenses card: no share bar, no %, counted in no total (negated but auditable), and its rows carry the same popover so a dispute can be moved back out (fixes the "no undo" gap from the 2026-07-27 report). Same-area category changes keep the rule-learning path; `cashflowView.txnCount` now counts active classes only so disputes can't make an empty month look real. Typecheck + lint clean; Alena verifies in her own instance.
- `[FILE]` Cash Flow gains a Saved section (Alena's ask: "income that became investments"). New `txn_class='saved'`: auto-detected when money leaves a cash account for an investment/savings destination (robinhood/vanguard/fidelity/schwab/401k/HYSA/"to savings"/… fingerprints in `lib/derive.ts`; wins over the generic transfer patterns, cash-side only so the mirror row on the excluded investment account can't double-count). One-time idempotent upgrade pass flips matching machine-made `'transfer'` rows — manual moves are never touched. `cashflowView` groups saved txns by destination; API passes `savedRows`/`totalSaved` (sheet months: empty); Saved card renders below Expenses with its own total + empty state. Txn ⋯ popover now moves transactions BETWEEN areas: Income/Saved pills apply instantly, Expense expands the category chips so nothing lands Uncategorized (PUT `/api/transactions` action `"move"`). Note: Saved tile (leftover = income − expenses) and Saved section (money actively moved) are different numbers by design. Typecheck + lint clean; not yet browser-verified — Alena reviews in her own instance per today's workflow rule.

### 2026-07-27 (later)

- `[OPS]` Killed an orphaned cloudflared quick-tunnel (PID 39447, up 3d5h, started by a long-finished session) that pointed at `http://127.0.0.1:3000`. It had been failing for some time — control-stream errors in a ~64s retry loop, public URL unreachable — so no live exposure at the time it was found. Target port was correct per convention: 3000 is demo (`run-dev.sh` hardcodes `FT_DEMO=${FT_DEMO:-1}`), so the real instance on 8420 was never tunnel-reachable. Audit prompted by a question about SSH/quantum hardening; noted in passing that SSH (GitHub push auth) is unrelated to Plaid/data security, and that the Mac already negotiates a post-quantum KEX with GitHub (`sntrup761x25519-sha512`) with an Ed25519 key — nothing to change there. See "Open security items" above for the real gap this surfaced.

- `[FILE]` **Cash Flow read-only + Monarch layout shipped.** Spec docs/superpowers/specs/2026-07-27-cashflow-readonly-monarch-design.md, plan docs/superpowers/plans/2026-07-27-cashflow-readonly-monarch.md, executed subagent-per-task. 8 commits: vitest harness (d1ef58e), cashflowView TDD 6 tests (67d75f2), API read-only GET/CashflowData + PUT/POST deleted (67ce6ed), view rewrite — tiles/allocation bar/drill-down rows (ccc51df), typography onto product-designer type scale (5403b82), income-green/expenses-red tiles (0971329), BudgetData dropped (df8c470), row amounts color-coded (dd54383). Owner-directed mid-flight: type-scale conformance, tile + row color coding. Verified live on demo: JUL 2026 derived and reconciling to the cent, drill-down expand/collapse single-open, sheet month 2026-01 stored + chevronless, APR 2026 empty state (no CTA, points at Connections), PUT/POST 405. Editing-loss bug closed by design: page never writes.
- `[FILE]` Post-review + owner-directed polish (fff743c): Expenses card sectioned NEEDS/WANTS (CashflowRow.grp), header renamed "Expenses", allocation bar semantic colors (needs red / wants faded red / saved green) with overspend-safe scaling + "Saved −N%" legend, savings-rate & saved tiles flip red when negative, row proportion bars colored per side, dead cashflowFromTransactions deleted. Final whole-branch review (fix-first verdict) satisfied: both Important overspend issues fixed; legacy stored-month fallback kept as spec'd (pre-migration months render read-only, chevronless — demo's JAN 2026 is one). Verified live post-fix.

### 2026-07-27

- `[OPS]` Bug-report triage (11 Replay reports, external QA agent): all recorded against a stale build — old muted color, old "Enter PIN" heading, value-only manual-asset edit, pre-derivation Cash Flow. Root cause of the reports themselves: an orphaned dev server from 2026-07-24 was still serving port 3000; killed and replaced with the harness-managed demo server, so the tunnel/demo now serves current code.
- `[OPS]` Re-verified every report live against the current build (loaded `127.0.0.1:3000` rather than `localhost:3000` — different cookie host, so the PIN gate renders locked without clearing an httpOnly session). **Confirmed fixed:** muted text contrast now 5.20:1 (passes WCAG AA; measured in-page); "Digits only — 6-digit PIN" hint on non-digit input; Unlock disabled until 6 digits; heading reads "Enter your 6-digit PIN"; wrong PIN returns 200 `{ok:false}` → inline "Wrong PIN", retryable, no Next.js forbidden page; Cash Flow renders derived with no empty state; manual-asset `Edit…` dialog exposes a name field, so rename works.
- `[OPS]` **BUG FOUND (open):** every Cash Flow edit is silently discarded on any month that has transactions. The card-switch auto-save works correctly (a PUT does fire), but `buildBudgetData` (`app/api/budget/route.ts:64-79`) re-derives from transactions and overwrites the just-saved values before responding. Proven via API: `PUT {fixed:[{label:"ZZZ-TEST",value:9999}]}` returns 200, yet both the response body and a subsequent GET return the original derived rows. The Cash Flow pencil/Edit UI is effectively decorative for covered months. Decision (Alena, same day): make Cash Flow read-only and drop the tax set-aside row — design spec pending.
- `[OPS]` Noted: manual-asset dialog inputs have no `aria-label` or `<label>` — placeholder-only accessible name. Minor a11y gap, not yet fixed.
- `[GIT]` Pushed to `github.com/alenabeex/plutus` (private): `1267472..69956a8`, 12 commits — Plaid item revoke on connection remove, orphan-row sweep (×2), `last_synced` stored as a real timestamp, blank install seeds categories only, demo `distDir` split (`.next-demo`, lets the real and demo servers run together), manual-asset icons + label edit, health-dot reasons (×2), mask disambiguation for duplicate account names, institution logo backfill on sync, lint/render-purity fixes. Code only — no financial data, no secrets.
- `[FILE]` Cash Flow redesign spec written + committed (docs/superpowers/specs/2026-07-27-cashflow-readonly-monarch-design.md): read-only page, Monarch-style — savings-rate tile, income allocation bar, category-level rows with inline drill-down; pencils/tax-row/PUT/POST removed. Pattern chosen by Alena from a Copilot/Monarch/Empower/YNAB/Mint comparison. Awaiting her spec review before implementation planning.
- `[FILE]` Created this log. Project activity split out of the vault's `00_system/activity.md`, which is for vault operations only (per Alena). Pre-2026-07-27 project entries left in place there as historical record.
